# Database — REMOTE ONLY (no local DB)
# Direct connection (requires server firewall to allow your IP):
DATABASE_URL="postgresql://chesstourism_user:PASSWORD@91.98.205.156:5432/chesstourism_db"
#
# OR via SSH tunnel (recommended for security):
# First run: ssh -L 5433:localhost:5432 root@91.98.205.156 -N &
# DATABASE_URL="postgresql://chesstourism_user:PASSWORD@localhost:5433/chesstourism_db"

PORT=3811
NODE_ENV=development
